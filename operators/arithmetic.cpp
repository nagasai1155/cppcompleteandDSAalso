#include <iostream>
using namespace std;

int main(){

    // float a = 3 , b = 4;

    //arithmetic operators 

    // cout<<"addition of a and b : " << a+b << endl;

    // cout << "substraction of a and b :" << (a-b) << endl;
    // cout << "multiplication of a and b :" << (a*b)<< endl;
    // cout<< "division of a and b " << (a/b)<< endl;
    //cout << "modulo of a and b " << (a%b) << endl;//modulo give error with float numbers 
   //unary operators 

   int x = 2;
   int y ;
   y = ++x;
   cout<<x <<endl<<y<<endl;

   y = x++;
   cout<<x<<endl<<y<<endl;

   //relational operators 
   //>,< ,==,!=,>=,<= these will return either 1 (true) and 0(false);



   



    return 0;
}