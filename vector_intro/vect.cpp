#include <iostream>
#include <vector>
using namespace std;

int main(){
    vector <int> numbers(5,0);//declaration vector and initialization with push_back function;
    // numbers.push_back(1);
    // numbers.push_back(2);
    // numbers.push_back(3);
    // numbers.push_back(4);
    // for(int i=0;i<numbers.size();i++){
    //     cout<<numbers[i]<<endl;
    // }

 for(int j=0;j<5;j++){
    cout<<numbers[j]<<endl;
 }

}